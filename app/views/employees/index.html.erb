<h1>Employees</h1>
<ul>
    
    <% @employees.each do |employee|%>
        <li><h3><%= link_to employee.to_s, employee_path(employee)%></h3></li>
    <% end %>

</ul>

  <%= button_to "Create new employee", new_employee_path, method: :get %>